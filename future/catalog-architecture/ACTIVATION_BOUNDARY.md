# Future Architecture Activation Boundary

Current state:

`PARKED`

This folder is documentation and planning data only.

It must have:

- zero frontend runtime imports
- zero backend runtime imports
- zero Firebase runtime dependency
- zero secret values
- zero deployment effect

Activation requires a separate future decision and feature branch after
the current Government Handloom + QR MVP production-readiness work is
closed.

Until then:

`DO_NOT_IMPLEMENT`
`DO_NOT_DEPLOY`
`DO_NOT_MERGE_INTO_RELEASE`
